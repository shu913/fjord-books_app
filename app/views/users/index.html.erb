<p><%= link_to "#{t('dictionary.booklist')}", books_path %></p>
<h1><%= t('dictionary.userlist') %></h1>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>

<%= paginate @users %>
